import { useState } from "react";

interface PropsTerrainTreeJs { 
    
}

export const TerrainTreeJs: React.FC<PropsTerrainTreeJs> = ({ }) => {
    return (
        <div>TerrainTreeJs</div>
    );
};